import Input from "../../components/Input";
import { Loading } from "../../components/Loading";

const Focus = () => {
  return (
    <div>
      <Loading />
    </div>
  );
};

export default Focus;
